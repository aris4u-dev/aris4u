# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.ruff_cache/
.mypy_cache/
venv/
dist/
build/
.pytest_cache/

# Models (large files)
models/base/
models/merged/
*.gguf
*.safetensors
*.bin
*.pt
*.pth

# Keep adapters (small)
!models/adapters/

# Data
data/raw/
*.jsonl.bak
# V16.7 cleanup: runtime DBs + claude-mem auto-generated context
data/sessions.db
data/exemplar_embeddings.npz
data/*.npz
AGENTS.md

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Secrets
.env
*.key
data/credentials/

# ARIS4U per-user config (generated by 'aris4u init', machine-specific)
.aris4u/
.aris-client

# Large binary files and databases
data/*.db
data/*.db-wal
data/*.db-shm
data/*.db.backup*

# Auto-adaptación: estado local del vigía (por-máquina, no versionar)
data/adapt_state.json
data/*.db.pre-*

# Codebase-Memory MCP (large binary, V16.6+)
.tools/codebase-memory/
data/*.db.bak*
data/*.db.reclassify-bak
data/*.db.bonds-bak
data/mode_s_*.db
data/mode_s_*.bin
data/mode_s_pre_*.db
data/knowledge_db/
data/.memory_key
data/snapshots/
data/soma_cache/
data/word_forge/
data/processed/
data/processed_filtered/
data/processed_strict/
data/ingest/
!data/funds_brain.db
!data/health_brain.db
!data/mac_brain.db
!data/business_brain.db

# Browser automation profile
browser/

# Old venvs
.venv312/

# LoRA adapters (83MB)
adapters/

# Logs
*.log

# Deployment replicas
deployment/
knowledge/security/practical/_archive/

# V10 — transient / credentials / binaries (added 0416c)
data/.claude_oauth_cache.json
data/supervisor.pid
data/session_state.json
data/dbos_sys.sqlite
data/dbos_app.sqlite
data/irc_bandit.sqlite
data/*.sqlite-journal
data/*.sqlite-wal
data/*.sqlite-shm

# Wave 4 archived binaries (too large for git — stored locally only)
data/archive_wave4/*.db
data/archive_wave4/*.bin
data/archive_wave4/*.json

# HNSW + embedding DBs (canonical — use backup mechanism, not git)
data/brain_hnsw.bin
data/brain_hnsw.bin.backup_nomic_768d
data/brain_hnsw_ids.json
data/brain_embeddings.db
data/brain.db
data/brain.db.bak_*
data/backups/
data/mode_s.db

# Spike / test artifacts
spikes/*/agent_*.sqlite
spikes/*/spike_*.sqlite
spikes/*/timeout_*.sqlite

# Model caches
.cache/
models/downloaded/
data/archive_knowledge/

# Session digests (generated, not tracked)
sessions/

# Large media files
*.webm
*.vtt
*.mp4

# V16.1 validation logs (runtime, not tracked)
logs/v16.1-events.jsonl
logs/lab-project-1-validation/
logs/*.state
logs/*.err
logs/*.out
logs/*.jsonl

# Hook backups from version migrations (archived locally)
hooks/.backup_*
hooks/_archive_*/
# V18 cleanup: pre-version backups never tracked
*.pre-v*

# V18 runtime artifacts (generated each session, not source code)
data/reports/
dashboard/data/
training/logs/
training/datasets/*.cache

# External wordlists / pentesting data (not our code)
tools/security-scripts/seclists/

# Mem0 local credentials (Phase 2a)
.env.mem0
logs/archive/

# V2.0 Fase 0 — runtime/cache no rastreable
logs/
data/*.npz.bak*
data/*.canary_ok
.codegraph/

# snapshot vivo del inventario (per-máquina, contiene el toolkit del usuario)
data/capability_runtime_snapshot.json

# caché de embeddings del router semántico (per-máquina, grande/binaria; refleja el
# toolkit del dueño) — se regenera con `python3 tools/capability_semantic.py --build`
data/capability_embeddings.npz
data/capability_routing_records.json

# backup de índice vectorial pre-migración occidental (46M, reversibilidad local)
data/aris_vectors.*.bak
data/*.bak
data/*.bak.*
data/*.bak-*

# backup pre-desacople claude-mem (V18 Fase E, 3GB local, ventana de seguridad)
data/_pre_decouple_bak/

# Material CVP/dual-use — NO en el distribuible (desminado V17 Tramo 0, 2026-07-02).
# Preservado en ~/Backups/cvp-material-20260702 + historia git. Purga total de historia = decisión aparte.
docs/v17-cvp/

# Serena IDE index
.serena/
CLAUDE.md.bak*
